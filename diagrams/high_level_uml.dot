@startuml

UI *--* SessionController

SessionController --> TaskMan

TaskMan --* Company
TaskMan --* TimeKeeper

Company --* ProjectManager
Company --* PlanningManager

ProjectManager --> "manages" Project
PlanningManager --> "makes" Planning

Project --> "manages" Task
Planning --> "is planning for" Task

Task --* TaskStatus


Interface UI

class SessionController

class TaskMan

class Company
class TimeKeeper

class ProjectManager
class PlanningManager

class Project
class Planning

class Task
class TaskStatus

@enduml
